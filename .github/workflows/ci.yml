name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: 'release'
      - name: Install R package tsfknn
        run: |
          Rscript -e "install.packages('tsfknn', repos='https://cloud.r-project.org')"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[dev]
          pip install isort black pytest rpy2 pandas numpy
      - name: Check formatting
        run: |
          isort --check tsknn tests
          black --check tsknn tests
      - name: Run tests
        run: pytest -q
