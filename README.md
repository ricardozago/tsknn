# tsknn

Time series k-nearest neighbors forecasting utilities.

## Distance metrics

The `tsknn` class supports several distance metrics via the `distance` parameter:

- `euclidean` (default)
- `manhattan`
- `chebyshev`
- `cosine`

